<section id="body">
  <h2><%= @page.title %></h2>
  <ul id="breads">
    <% @breads.each do |bread| %>
      <% if bread.name %>
        <li>
          <%= link_to bread.name, refinery.breads_bread_path(bread) %>
        </li>
      <% end %> 
    <% end %>
  </ul>
</section>
