<%= render 'refinery/admin/search_header', :url => refinery.breads_admin_breads_path %>
<div class='pagination_container'>
  <% if @breads.any? %>
    <%= render 'breads' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
