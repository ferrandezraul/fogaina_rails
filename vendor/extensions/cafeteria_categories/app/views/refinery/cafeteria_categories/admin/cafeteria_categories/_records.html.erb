<%= render 'refinery/admin/search_header', :url => refinery.cafeteria_categories_admin_cafeteria_categories_path %>
<div class='pagination_container'>
  <% if @cafeteria_categories.any? %>
    <%= render 'cafeteria_categories' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
