<li class='clearfix record <%= cycle("on", "on-hover") %>' id="<%= dom_id(video) -%>">
  <span class='title'>
    <%= video.title %>
  </span>

  <span class='preview'></span>

  <span class='actions'>
    <%= action_icon(:preview, refinery.videos_video_path(video), t('.view_live_html')) %>
    <%= action_icon(:edit,    refinery.edit_videos_admin_video_path(video), t('.edit') ) %>
    <%= action_icon(:delete,  refinery.videos_admin_video_path(video), t('.delete'),
      { class: "cancel confirm-delete",
        data: {confirm: t('message',  scope: 'refinery.admin.delete', title: video.title)}}  ) %>
  </span>
</li>
